export default function App() {
  return (
    <div className="min-h-screen bg-[#0a0a0b] text-zinc-100 antialiased">
      <header className="border-b border-white/10 px-6 py-4">
        <div className="mx-auto flex max-w-6xl items-center justify-between">
          <span className="text-sm font-semibold tracking-tight">New Project</span>
          <nav className="flex gap-4 text-xs text-zinc-400">
            <span>Energy</span>
            <span>Focus</span>
            <span>Recovery</span>
          </nav>
        </div>
      </header>
      <main className="mx-auto max-w-6xl px-6 py-16">
        <p className="text-xs uppercase tracking-[0.2em] text-violet-400">Dark SaaS prototype</p>
        <h1 className="mt-3 text-4xl font-semibold tracking-tight">New Project</h1>
        <p className="mt-4 max-w-2xl text-zinc-400">@cto /src/components/home-content.tsx: Link is not defined (30:15)

[Canvas directive: Break down the new information above into distinct board nodes. Emit one </p>
      </main>
    </div>
  );
}
